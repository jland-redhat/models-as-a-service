apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../after
  # Pre-auth model extraction is inlined in envoy-filter.yaml
  # (json_to_metadata + Lua) — no payload-pre-processing Deployment.
  - envoy-filter.yaml
  - plugins-configmap.yaml

images:
  - name: payload-processing
    newName: quay.io/opendatahub/odh-ai-gateway-payload-processing
    newTag: odh-stable
