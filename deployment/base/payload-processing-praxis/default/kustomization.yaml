apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Parallel Praxis-based IPP stack (inactive). Production overlays still point at
# deployment/base/payload-processing. Wire via Config profile / overlay switch later.
resources:
  - ../manager
  - ../rbac
  - ../networking

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: models-as-a-service
      app.kubernetes.io/component: payload-processing
      app.kubernetes.io/name: payload-processing
